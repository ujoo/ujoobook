<html>
<head>
<% include ./headerHtml.ejs %>
</head>
<body>
<% include ./header.ejs %>

<div class="container">
    <div class="row">
        <div class="col-lg-12">

            <div class="bs-component">
                <table class="table table-striped table-hover ">
                <tbody>
                    <tr>
                        <td>
                            <%=moment().format("YYYY-MM-DD")%>일자 <%=title%>
                        </td>
                    </tr>
                    <% contents.forEach(function(data, index) { %>
                    <tr>
                        <td><%=data.title%> <%=data.time%></td>
                    </tr>
                    <% }) %>
                </tbody>
                </table> 
            </div>
        </div>

    </div>

    <% include ./footer.ejs %>


</div>

    <script type="text/javascript">
        $(document).ready(function() {
        });
    </script>
</body>
</html>